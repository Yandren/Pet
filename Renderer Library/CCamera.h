#ifndef __CCAMERA_H
#define __CCAMERA_H
/*
#include "glm\glm\glm.hpp"
#include <string>

enum Direction { RIGHT, LEFT, FORWARD, BACKWARD };

class CCamera {

public:

  CCamera();
  ~CCamera(){};
  float getAspectRatio();
  glm::mat4 getViewMatrix();
  bool moveCam(int key, float timeTick);

  glm::vec3 mPosition; //camera position in space
  glm::vec3 mUpOrientation; //how it's held - aka "Up" vector OR could be pitch/yaw/roll
  glm::vec3 mDirection; //where it's aimed
   
  int mViewportHeight;
  int mViewportWidth;
  float mToClipPlaneNear;
  float mToClipPlaneFar;
  std::string mName;
  float mMoveSpeed;

};
*/
#endif